@inject ApiClient ApiClient

<div class="login-container">
	<div class="login-card">
		<div class="login-header">
			<div class="login-logo">⚡</div>
			<h2>Welcome Back</h2>
			<p class="login-subtitle">Sign in to your admin account</p>
		</div>

		<EditForm Model="@_loginModel" OnValidSubmit="HandleLogin" class="login-form">
			<DataAnnotationsValidator />
			
			<div class="form-group">
				<label for="username">Username</label>
				<div class="input-wrapper">
					<span class="input-icon">👤</span>
					<InputText id="username" @bind-Value="_loginModel.Username" placeholder="Enter your username" class="form-input" />
				</div>
				<ValidationMessage For="@(() => _loginModel.Username)" />
			</div>
			
			<div class="form-group">
				<label for="password">Password</label>
				<div class="input-wrapper">
					<span class="input-icon">🔒</span>
					<InputText id="password" @bind-Value="_loginModel.Password" type="password" placeholder="Enter your password" class="form-input" />
				</div>
				<ValidationMessage For="@(() => _loginModel.Password)" />
			</div>

			@if (!string.IsNullOrEmpty(_errorMessage))
			{
				<div class="error-message">
					<span>⚠️</span> @_errorMessage
				</div>
			}

			<button type="submit" class="login-button" disabled="@_isLoading">
				@if (_isLoading)
				{
					<span>Signing in...</span>
				}
				else
				{
					<span>Sign In</span>
				}
			</button>
		</EditForm>
		
		<div class="login-footer">
			<p>SuperAdmin Panel v1.0</p>
		</div>
	</div>
</div>