@page "/companies"
@using WebWasm.Components

<PageTitle>Companies</PageTitle>

<div class="page-header">
	<h1>🏢 Companies</h1>
	<p class="page-subtitle">Manage registered companies</p>
</div>

<div class="content-card">
	<div class="card-header">
		<h3>Company List</h3>
		<button class="btn-primary" @onclick="OpenAddCompanyModal">+ Add Company</button>
	</div>
	<CompaniesTable @ref="_companiesTableRef"
					Companies="@_companies"
					OnToggleActive="@HandleToggleActive"
					OnEditCompany="@HandleEditCompany"
					OnDeleteCompany="@HandleDeleteCompany"
					OnEditSecurityLevel="@HandleEditSecurityLevel" />
</div>

<CompanyModal IsOpen="@_isCompanyModalOpen"
			  EditingCompany="@_editingCompany"
			  OnClose="@CloseCompanyModal"
			  OnSubmit="@HandleCompanySubmit" />

<SecurityLevelDialog IsOpen="_showSecurityLevelModal"
					 EntityLabel="@($"Company: {_securityLevelTargetCompany?.Name}")"
					 CurrentLevel="_securityLevelCurrent"
					 OnClose="CloseSecurityLevelModal"
					 OnSubmit="ConfirmSecurityLevel" />

<ConfirmDialog IsOpen="@_showConfirmDialog"
			   Title="@_confirmTitle"
			   Message="@_confirmMessage"
			   ConfirmText="Confirm"
			   CancelText="Cancel"
			   OnConfirm="@HandleConfirm"
			   OnCancel="@CloseConfirmDialog" />